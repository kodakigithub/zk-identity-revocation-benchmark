{
  "name": "@zkcr/circuits",
  "version": "0.1.0",
  "private": true,
  "description": "Circom circuits: shared attribute predicate + nullifier and SMT non-membership revocation variants",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "export PATH=\"$PWD/../tools/bin:$PATH\" && mocha test/*.test.js --timeout 300000"
  },
  "devDependencies": {
    "circom_tester": "^0.0.19",
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "mocha": "^10.7.3",
    "snarkjs": "^0.7.5"
  }
}
